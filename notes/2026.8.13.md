# AI 产品从入门到精通 学习笔记
2026.8.12
## 大模型原理 大模型幻觉
1.大模型的幻觉说到底就像后背写字接龙：每个人只负责把前面的话"顺下去"，越往后传，离原意越远。模型也一样。你问"吃了吗"，它回答"吃了"——不是因为真吃了，而是"吃了"这个词在"吃了吗"后面概率最高。它只是在玩一场字词概率接力，选"最顺"的接，不是"最对"的接。模型没有"不知道"的概念。参数里没答案时，它会生成"听起来应该是这样"的内容，而且说得很自信——置信度不因信息不准确而下降。

2.应对的办法主要有四种：

（1）.提示词工程的约束，是给模型加一道护栏：遇到知识盲区或模糊问题时，优先说"我不确定"，而不是流畅地编造，虽然它不能让模型变聪明，但是提高了"承认不知道"的概率，以及压低了"一本正经胡说"的概率。

（2）.通过 RAG 把真实文档片段塞进上下文，让模型从"有据可查"的地方开始接话，而不是凭空编造：技术问题能查到内部文档，用药助手能翻到说明书原文，法律助手能定位到具体法条，出处可查，错了也能追责。"有据可查"是 RAG 的核心价值，但"可查"不等于"必对"；但源头本身可能错，模型理解也可能歪。
RAG 是给模型开卷考试，但开卷不代表能答满分，书可能找错，答案可能抄串行，它的价值是有据可查，不是一定正确。
但开卷考试要先印卷子（文档向量化）、要有人翻书（向量检索）、考场上还要发卷子（Prompt 增大塞检索结果）。印一次便宜，但每考一次都要发一次卷子，发多了就贵，可以通过以下四种策略，在省钱和提高准确率之间做取舍。
| 成本项 | 贵不贵 | 说明 |
|--------|--------|------|
| 文档向量化 | 低（一次性） | 入库时跑一遍，之后复用 |
| 问题向量化 | 低 | 每次查询约 0.1 元/百万 Token |
| 向量检索 | 中 | 知识库大了会慢 |
| Prompt 增大 | 高 | 每次多塞 500-2000 Token，LLM 费用翻倍 |

| 策略 | 省什么 | 核心思想 |
|------|--------|---------|
| 关键词触发 | 跳过 30-70% 查询 | "今天几号"这种简单问题，直接答 |
| 模型路由 | LLM 费用降 60-80% | 简单问题用小模型（便宜），难的才上旗舰 |
| 语义缓存 | 高频查询降 50% 延迟和费用 | "退款政策"和"怎么退款"结果一样，复用 |
| 精准切块 | 准确率提高 20-40% | 文档切太大塞冗余 Token，切太小丢上下文 |

（3）.通过调节小T（Temperature）和小P（Top-P）的数值来改变模型采样时的概率分布形状：数值低，分布变尖，输出稳定保守；数值高，分布变平，输出灵活发散。
它们是全局参数，就像整车喷漆，不是局部补漆。想要"部分保守、部分发散"，拆任务比调参数更靠谱。

（4）.最后一步，当前面的三个步骤都防不住时，需要人工来进行兜底，就像食品厂，目标是"细菌数低于国标"，不是"无菌"。追求消灭幻觉不现实，追求可量化、可监控、可兜底才现实。

从发现幻觉到应对幻觉的三个环节：上线前建测试集摸底，上线后高风险人工审，持续收集 Bad Case 反哺迭代。虽然不能满分有效的把幻觉扼杀，但是能最大限度压制幻觉的肆意妄为。
