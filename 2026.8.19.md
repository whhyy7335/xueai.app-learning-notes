# AI 产品从入门到精通 学习笔记
2026.8.19
## HARNESS核心 Al Harness

### Prompt工程 为什么是Markdown

**1. 在大模型的训练语料中Markdown的占比本来就最多**

大模型学的最多的是互联网上的语料：GitHub、技术博客、官方文档，大量以 Markdown（或与它同构的 HTML）存在。模型对 `#`、`-`、`**` 这些符号的"语感"是天然训练出来的。输出 Markdown 几乎零成本，是它最顺手的"母语"，像打字时顺手敲回车分段。

**2. Token 效率**
| 方案 | 为什么不行 |
|------|-----------|
| HTML | 标签太重，`<div>` `<p>` 浪费大量 Token |
| Word/PDF | 二进制格式，模型无法逐字生成 |
| LaTeX | 语法复杂，模型容易出错 |
| Markdown | `#` `​**​` `` ` `` 只占几个 Token，纯文本兼容，前端一行代码渲染 |


每个 token 都是宝贵的钱和时间。课程里也提到，Markdown 装饰全删能省 15-40% Token，反过来说明它本身比 HTML 之类的格式轻得多。

**3. 人机两吃的中间态**

JSON 流式输出时残缺、无法实时解析；Markdown 半截标题也能先渲染出来，二进制包装好可以是pdf，通俗易懂能打印，但是对于AI来说，把这一堆二进制内容读出来可得费不少时间和精力。而且 Markdown 容错率高，解析失败内容还能看，JSON 解析失败就整个废了。

### 结论

不是"工程师规定模型用 Markdown"，而是它在"训练语料最熟 + Token 最省 + 人机两头都能接住"三个约束下收敛出来的最优解。真要纯给程序消费的场景，工程师还是会让它吐 JSON。

### 工程选型
大模型输出的是纯文本 Markdown，前端需要把它变成富文本。

| 场景 | 推荐库 |
|------|--------|
| 轻量项目 | marked.js（6KB gzip） |
| React 项目 | react-markdown |
| 需要公式/图表 | markdown-it |

输出格式场景适配有以下这几种类型

| 格式 | 特点 | 适合场景 |
|------|------|---------|
| TXT 纯文本 | 流式直接呈现，最稳定 | 简单回答 |
| JSON | 结构化，流式残缺无法实时解析 | 后端程序消费 |
| Markdown | 流式可渲染，结构提取难 | 文档/富文本展示 |
| YAML | 省 Token，流式同样残缺 | 配置、简单数据结构 |
| XML/自定义标签 | 流式可增量解析，最友好 | 程序间通信、需要实时处理 |

### SSE 流式渲染与未闭合标记
大模型通过 SSE 逐字推送 Markdown 文本时，前端拿到的常常是语法不完整的片段。
以加粗标记 ：​**今天天气很好**​为例，渲染器在流式过程中会依次遇到：

```mermaid
flowchart LR
    T1["收到 `*`"] --> T2["凑成 `​**​`"]
    T2 --> T3["收到 `今`"]
    T3 --> T4["收到 `天天气很`"]
    T4 --> T8["补上 `​**​`，渲染成粗体"]:::done

    classDef done fill:#4ade80,stroke:#16a34a,color:#052e16;
```

问题在于 t1 到 t7 之间，渲染器拿着半截语法，无法确定后续内容该按正文还是按加粗处理。不同库表现不同：有的把未闭合标记当普通文本，等闭合时才"啪"地变粗，造成跳动闪烁；有的把后续整段都当成加粗，直到闭合才恢复，视觉上错乱得更明显。列表、代码块同理，列表的圆点缩进、代码块的灰底等宽字体，都要等闭合标记到达后才会一次性出现，看起来就是"迟到的突变"。
为了解决这个问题，通常会用两个方法
**节流**：不每收到一个字符就重渲染，攒一小段时间（约 50~100ms）统一渲染一次，减少突变频率。
**补全**：把攒下的文本交给渲染器前，先检测末尾是否有未闭合的标记（`**`，`-`等），自动补上闭合符号。渲染器因此始终拿到语法完整的文本，稳定渲染，等真正的闭合标记到达时再替换掉补全部分。

