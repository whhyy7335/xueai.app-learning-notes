# curl 学习笔记
2026.8.24
## 1.curl 是什么有什么用
curl 是一种用于通过 URL 与服务器之间传输数据的工具，可以理解为curl是匡衡凿壁偷光的凿子，而服务器是邻居家，URL是墙上你要凿的那个具体位置（通往服务器的地址）；
就比如github这个邻居，我想看看它家里有什么，可以这样：
```curl https://github.com/```那么就可以得到:
```
<!DOCTYPE html>
<html
...
  </body>
</html>
```
一长串，完整的，50多万个字符的网页源码，看起来极其复杂，根本不知道怎么获取有效信息，看起来就像是凿到了承重墙，而不是特定的某个有光源或者是可以固定观察的地方，那么则需要加一些手法和角度，如下面公式所示：
```curl [选项 / URL 地址]```
选项又是什么，选项是以一个或两个连字符开头，如果输入的文本不以连字符开头，那么该文本将被视为 URL 并据此处；许多选项旁边还需要再输入一个值。简单来说就是需要控制的角度和力度；curl 官方文档里列了 200 多个可用的选项，但其实真正日常用到的就那么几个，可以做成以下几个分类：
### 一、上奏（拿到数据后，怎么呈现给你）

`-o 文件名`  它的作用是把结果保存到指定文件，就像拿着一根魔法棒整理图书馆的书一样；

 `-O`  它的作用是按 URL 原本的文件名保存下载文件 

 `-o NUL` 意思是把返回的数据写进 NUL

 `-s`  静默，去掉在cmd里执行的进度条，告诉手下“别说话，只管做”

 `-I`  只拿响应头  把HTTP响应想成一个快递，开箱之前核对货单的号码是否正确；**阴的来了，`I`和`i`是两个不同的选项**；

 `-i`  拿响应头和网站源码，依旧把HTTP响应想成一个快递，除了核对货单号，还要开箱验货 ；

 `-w "%{http_code}\n"`极其霸道的一个选项，它允许你从 curl 内部拿一些变量，然后自己设计最终报告，`%{http_code}`就是其中一个变量，还有以下这些常用的变量：
 | 变量 | 它告诉你什么 | 例子 |
| --- | --- | --- |
| `%{http_code}` | HTTP 状态码 | `200`、`404`、`500` |
| `%{time_total}` | 整个请求总耗时 | `0.382` 秒 |
| `%{time_starttransfer}` | 多久收到服务器第一个字节 | `0.214` 秒 |
| `%{remote_ip}` | 实际连接到的服务器 IP | `93.184.216.34` |
| `%{remote_port}` | 服务器端口 | `443` |
| `%{local_ip}` | 你这边使用的 IP | `192.168.1.8` |
| `%{size_download}` | 下载了多少字节 | `1256` |
| `%{size_upload}` | 上传了多少字节 | `128` |
| `%{speed_download}` | 平均下载速度 | 字节/秒 |
| `%{url_effective}` | 最终真正访问的 URL | 跳转后特别有用 |
| `%{num_redirects}` | 经历了几次重定向 | `2` |
| `%{http_version}` | 使用的 HTTP 版本 | `1.1`、`2`、`3` |
| `%{method}` | 实际使用的请求方法 | `GET`、`POST` |
| `%{content_type}` | 返回内容类型 | `text/html`、`application/json` |

依旧看着极其复杂，举一个简单的例子，我想要知道B站的状态，上站的耗时，和服务器ip，于是开始吟唱：
`curl -s -o NUL -w "状态码: %{http_code}\n耗时: %{time_total}s\n服务器IP: %{remote_ip}\n" https://www.limestart.cn/`
返回的结果如下：
`状态码: 200`<br>`耗时: 0.299175s`<br>`
服务器IP: 183.232.239.18
`
翻译成中文就是：curl，帮我访问B站`https://www.limestart.cn/`。访问过程中别显示进度信息`-s`，网站返回的网页内容我也不要，我只要数值`-o NUL`。`-w`等访问结束后，只告诉我：HTTP 状态码` %{http_code}`是多少、整个请求花了多少秒`%{time_total}`、最后连接到的服务器 IP `%{remote_ip}`是什么。(换行是\n)

### 二、请求本身（你跟服务器说什么）

| 选项 | 作用 | 典型场景 |
|------|------|----------|
| `-X GET/POST/...` | 指定请求方法 | 调接口（读 / 写） |
| `-d "内容"` | 提交数据 | 提交表单 / JSON |
| `-H "Key: value"` | 加请求头 | 带 token、伪造成浏览器 |
| `-L` | 自动跟随重定向 | 服务器说去那边，你跟上 |

### 三、连接过程（怎么处理这次传输）

| 选项 | 作用 | 典型场景 |
|------|------|----------|
| `-v` | 显示每一步过程 | 看 DNS、TCP、TLS、发请求、收响应 |
| `-m 秒数` | 最多等待多少秒 | 防止卡死 |
| `--connect-timeout 秒` | 连接超时 | 连不上时快速放弃 |

### 四、身份认证（告诉服务器你是谁）

| 选项 | 作用 | 典型场景 |
|------|------|----------|
| `-u 用户名:密码` | 基础认证 | 带账号密码的接口 |
| `-b "cookie值"` | 携带 cookie | 模拟已登录状态 |
